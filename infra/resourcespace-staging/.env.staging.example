# Copy to .env on the ResourceSpace staging VM. Do not commit real .env.

TZ=UTC
RESOURCESPACE_HTTP_PORT=8080

MARIADB_DATABASE=resourcespace
MARIADB_USER=resourcespace
MARIADB_PASSWORD=replace-with-long-random-password
MARIADB_ROOT_PASSWORD=replace-with-long-random-root-password

RESOURCESPACE_PUBLIC_URL=https://dam-staging.tjc.org
